# Frontend placeholder

The Vue 3 + TypeScript + Vite application will be initialized in Phase 2. Keeping
this directory now makes the seven planned top-level business modules visible
without introducing frontend code before the backend contracts are stable.
